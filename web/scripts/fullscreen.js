var elem1 = document.getElementById("image1");
function openFullscreen1() {
    if (elem1.requestFullscreen && !document.fullscreenElement) {
        elem1.requestFullscreen();
    }
    else if (document.exitFullscreen && document.fullscreenElement) {
        document.exitFullscreen();
    }
}
var elem2 = document.getElementById("image2");
function openFullscreen2() {
    if (elem2.requestFullscreen && !document.fullscreenElement) {
        elem2.requestFullscreen();
    }
    else if (document.exitFullscreen && document.fullscreenElement) {
        document.exitFullscreen();
    }
}
var elem3 = document.getElementById("image3");
function openFullscreen3() {
    if (elem3.requestFullscreen && !document.fullscreenElement) {
        elem3.requestFullscreen();
    }
    else if (document.exitFullscreen && document.fullscreenElement) {
        document.exitFullscreen();
    }
}
var elem4 = document.getElementById("image4");
function openFullscreen4() {
    if (elem4.requestFullscreen && !document.fullscreenElement) {
        elem4.requestFullscreen();
    }
    else if (document.exitFullscreen && document.fullscreenElement) {
        document.exitFullscreen();
    }
}
var elem5 = document.getElementById("image5");
function openFullscreen5() {
    if (elem5.requestFullscreen && !document.fullscreenElement) {
        elem5.requestFullscreen();
    }
    else if (document.exitFullscreen && document.fullscreenElement) {
        document.exitFullscreen();
    }
}
